{
  "framework": null,
  "buildCommand": "cd server && pnpm install && pnpm build",
  "outputDirectory": null,
  "installCommand": "pnpm install",
  "regions": ["iad1"],
  "functions": {
    "server/**/*.ts": {
      "runtime": "nodejs24.x",
      "memory": 512,
      "maxDuration": 30
    }
  }
}
