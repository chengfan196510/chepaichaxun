{
  "version": 2,
  "builds": [
    {
      "src": "server/api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/api/$1"
    }
  ],
  "env": {
    "EXPO_PUBLIC_BACKEND_BASE_URL": "/api"
  }
}
